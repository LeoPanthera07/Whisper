{
  "name": "offline-chat-app",
  "version": "1.0.0",
  "description": "Offline Wi-Fi based secure chat app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:exe": "npm run build:ui && node embed-assets.js && npx pkg server.js --target node18-win-x64 --output Whisper.exe --compress GZip",
    "build:ui": "cd client && npm run build && cd .."
  },
  "pkg": {
    "assets": [
      "embedded-assets.js",
      "node_modules/qrcode-terminal/**/*"
    ],
    "targets": [
      "node18-win-x64"
    ]
  },
  "dependencies": {
    "express": "^4.19.2",
    "qrcode-terminal": "^0.12.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}
